name: CI

on:
  pull_request:
    branches: [main]
  push:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: '1.25'
      - name: Run tests
        run: go test -race -count=1 ./...
      - name: Run vet
        run: go vet ./...
      - name: Build all packages
        run: go build ./...
      - name: Build benchmark binary
        run: go build -o /dev/null ./turbo-bench/
